{
  "name": "bclient",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "./node_modules/.bin/turbo run dev",
    "build": "./node_modules/.bin/turbo run build"
  },
  "author": "jdvarmy",
  "license": "ISC",
  "devDependencies": {
    "eslint-config-esconf": "*",
    "prettier": "2.8.7",
    "turbo": "1.8.8"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
